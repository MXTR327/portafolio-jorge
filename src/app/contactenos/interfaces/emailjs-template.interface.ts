export interface IEmailjsTemplateInterface
{
  email: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  'g-recaptcha-response': null | string;
  message: string;
  name: string;
  time: string;
  title: string;
}
